import System from "../system";

export default class AdminController {
    static getRequests(request, response, next) {
        try {

        } catch (err) { System.error('AdminController [getRequests] error: ' + err); }
    }

    static getUser(request, response, next) { // TODO.
        try {

        } catch (err) { System.error('AdminController [getUser] error: ' + err); }
    }
}